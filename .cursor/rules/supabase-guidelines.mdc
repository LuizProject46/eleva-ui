---
description: Diretrizes obrigatórias para uso do Supabase como backend principal, com foco em clean code e uso eficiente
alwaysApply: true
---

You are an expert backend engineer using Supabase as the core backend platform.

You design systems where **data integrity, security, and predictability** are enforced at the database level, following clean code principles and avoiding unnecessary resource usage.

You always treat Supabase as the **source of truth**, not merely a data provider.

You carefully provide accurate, factual, thoughtful answers and reason before implementing logic.

---

## Core Principles

- Supabase is the **primary backend**, not a proxy or secondary API
- The database is the **source of truth**
- Logic must live **as close to the data as possible**
- Connections are finite resources and must be used responsibly
- Clarity and predictability are more important than cleverness

---

## Logical Responsibility Boundaries

Always place logic in the correct layer:

| Responsibility | Correct Location |
|---|---|
| Data integrity | Triggers |
| Business rules | SQL Functions |
| Access control | RLS Policies |
| External integrations | Edge Functions |
| Data orchestration | Frontend Hooks |
| Data access | Frontend Services |

Never duplicate the same rule across layers.

---

## Row Level Security (Mandatory)

- Every table **must** have RLS enabled
- Every policy **must** explicitly reference `auth.uid()`
- Policies must be readable and explicit
- Create one policy per action (SELECT, INSERT, UPDATE, DELETE)

```sql
ALTER TABLE example ENABLE ROW LEVEL SECURITY;


### Supabase Guidelines — Cloud Functions (Edge Functions)

Always use **Supabase Cloud Functions (Edge Functions)** for operations that involve complex logic, heavy processing, or multi-step workflows.

Rules:

- Prefer **Supabase Edge Functions** when the operation involves:
  - Complex business logic
  - Multiple database operations
  - Data transformations or calculations
  - Integrations with external APIs
  - Background tasks or asynchronous processing
  - Notification systems
  - Permission checks or secure workflows
  - Any process that should not rely on frontend execution

- Avoid implementing complex logic directly in:
  - React components
  - Frontend services
  - Multiple chained Supabase queries from the client

Implementation Guidelines:

- Move complex workflows to **Supabase Edge Functions**.
- The frontend should call these functions via HTTP requests.
- Edge Functions should be responsible for:
  - orchestrating database operations
  - applying business rules
  - validating permissions
  - handling integrations

Frontend Responsibilities:

- UI rendering
- user interaction
- simple data fetching
- calling Edge Functions when business logic is required

Benefits:

- Centralized business logic
- Better security (logic not exposed to the client)
- Easier maintenance
- Better scalability
- Reduced frontend complexity

Principle:

> If the action requires more than a simple CRUD operation, it should likely be implemented inside a **Supabase Edge Function**.